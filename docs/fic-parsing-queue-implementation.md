# Fic Metadata Queue

This page has moved.

The fic metadata queue is fully implemented, so the canonical documentation now lives here:

- `docs/fic-metadata-queue.md`

This stub exists to avoid breaking old links.
